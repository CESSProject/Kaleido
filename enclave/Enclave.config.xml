<!-- Please refer to User's Guide for the explanation of each field -->
<EnclaveConfiguration>
  
  <!-- ISV assigned Product ID -->
  <ProdID>0</ProdID>  
  
  <!-- ISV assigned SVN -->
  <ISVSVN>0</ISVSVN>  
  
  <!-- The stack size per trusted thread (in bytes) --> 
  <StackMaxSize>0x40000</StackMaxSize>  
  
  <!-- 256MB The heap size for the enclave (in bytes)--> 
  <HeapMaxSize>0x20000000</HeapMaxSize>

  <!-- 3GB The heap size for the enclave (in bytes)--> 
  <!-- <HeapMaxSize>0xC0000000</HeapMaxSize>   -->
  
  <!-- 8GB The heap size for the enclave (in bytes) -->
  <!-- <HeapMaxSize>0x200000000</HeapMaxSize> -->
  
  <!-- The number of trusted threads -->
  <TCSNum>10</TCSNum>  

  <!-- TCS management policy -->
  <TCSPolicy>0</TCSPolicy>  

  <!-- Set it to 1 in production -->
  <DisableDebug>0</DisableDebug>

  <!-- Reserved for future use. Must be 0 --> 
  <MiscSelect>0</MiscSelect>  

  <!-- Reserved for future use. Must be 0xFFFFFFFF. -->
  <MiscMask>0xFFFFFFFF</MiscMask> 
</EnclaveConfiguration>
